export type LintResult = {
	ok: boolean,
	group: string,
	statusMessage: string,
	subject: Instance?,
}

export type LintResultPartial = {
	ok: boolean,
	statusMessage: string,
	subject: Instance?,
}

return nil
